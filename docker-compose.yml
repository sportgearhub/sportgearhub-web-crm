services:
  sportgearhub-web-provider:
    container_name: sportgearhub-web-provider
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${WEB_PROVIDER_HTTP_PORT:-8080}:80"
    networks:
      - apps-proxy

networks:
  apps-proxy:
    external: true
